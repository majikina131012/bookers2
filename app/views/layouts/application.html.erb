<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/"><h1>Bookers</h1></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="true" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse show" id="navbarNavDropdown" style="">
          <ul class="navber-nav ml-auto list-unstyled">
          <% if user_signed_in? %>
            <li>
              <%= link_to user_path(current_user), class:"nav-link text-light" do %>
                <i class="fas fa-home"></i> <%= t('nav.Home') %>
              <% end %>
            </li>
            <li>
              <%= link_to users_path, class:"nav-link text-light" do %>
                <i class="fas fa-users"></i> <%= t('nav.Users') %>
              <% end %>
            </li>
            <li>
              <%= link_to books_path, class:"nav-link text-light" do %>
                <i class="fas fa-book-open"></i> <%= t('nav.Books') %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete, class:"nav-link text-light" do %>
                <i class="fas fa-sign-out-alt"></i> <%= t('nav.Logout') %>
              <% end %>
            </li>
          <% else %>
            <li>
              <%= link_to root_path, class:"nav-link text-light" do %>
                <i class="fas fa-home"></i> <%= t('nav.Home') %>
              <% end %>
            </li>
            <li>
              <%= link_to about_path, class:"nav-link text-light" do %>
                <i class="fas fa-link"></i> <%= t('nav.About') %>
              <% end %>
            </li>
            <li>
              <%= link_to new_user_registration_path, class:"nav-link text-light" do %>
                <i class="fas fa-user-plus"></i> <%= t('nav.Sign In') %>
              <% end %>
            </li>
            <li>
              <%= link_to new_user_session_path, class:"nav-link text-light" do %>
                <i class="fas fa-sign-in-alt"></i> <%= t('nav.Log In') %>
              <% end %>
            </li>
          <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <main>
    <div class="container px-5 px-sm-0">
      <h6><%= flash[:notice] %></h6>
      <%= yield %>
    </div>
  <main>
</body>
<footer>
  <div class="container mt-5">
    <div class="row">
      <div class="mx-auto">
        <select id="language-select" class="form-control">
          <option value="en" <%= 'selected' if I18n.locale == :en %>>English</option>
          <option value="ja" <%= 'selected' if I18n.locale == :ja %>>日本語</option>
        </select>
        <script>
          document.getElementById('language-select').addEventListener('change', function() {
            var selectedLocale = this.value;
            window.location.href = "/change_locale/" + selectedLocale;
          });
        </script>
      </div>
    </div>
    <div class="row">
      <div class="mx-auto">
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>
</footer>
</html>
